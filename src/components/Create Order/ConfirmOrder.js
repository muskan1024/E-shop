import React from "react";

const ConfirmOrder = () => {
  // Display order details and confirm the order
  return (
    <div>
      {/* Display order details */}
      <button>Confirm Order</button>
    </div>
  );
};

export default ConfirmOrder;
